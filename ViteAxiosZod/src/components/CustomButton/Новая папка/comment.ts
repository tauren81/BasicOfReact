/*
import axios from "axios"
import { CommentDTO, CommentSchema } from "./commentSchema"

export const fetchComments = async (userId: string): Promise<CommentDTO> => {
    const response = await axios(https://jsonplaceholder.typicode.com/posts/${userId});
    return CommentSchema.parse(response.data);
}   
    */
