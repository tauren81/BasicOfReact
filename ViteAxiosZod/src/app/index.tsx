import CustomButton from '@components/CustomButton/CustomButton';

function App() {
  return (
    <div>
      <h3>
        <CustomButton />
      </h3>
      {/*
      <div>
        <ul>
          {data.map((record, i) => (
            <li key={i}>{record.title}</li>
          ))}
        </ul>
      </div>
      */}
    </div>
  );
}

export default App;
